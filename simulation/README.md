# buddies (simulation)

FDTD simulation of underwater acoustics.

- `src/buddies/`: the simulation engine and CLI (`buddies ...`).
- `experiments/`: individual simulation definitions (`<name>/simulate.py`, optional `view.py`).
- `tools/`: analysis and reporting helpers.

Run from this directory, e.g. `uv run buddies show <experiment>`.
