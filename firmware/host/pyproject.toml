[project]
name = "buddies-fw-host"
version = "0.1.0"
description = "Host-side tooling for buddies firmware (harness, listener)"
requires-python = ">=3.12"
dependencies = [
    "PySide6>=6.7",
    "numpy>=2.0",
]

[tool.uv]
package = false
